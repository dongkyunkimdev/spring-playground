playground:
  title: Config Server
  version: 1.0.0

server:
  port: 8888

spring:
  profiles:
    active:
      - native
  application:
    name: config-server
  cloud:
    config:
      server:
        native:
          search-locations:
            - classpath:config/
